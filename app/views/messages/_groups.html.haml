.side-header
  %p.side-header__text
    = current_user.name
  = link_to edit_user_path(current_user) ,class: "side-header__btn" do
    =fa_icon 'cog'
  = link_to new_group_path ,class: "side-header__btn" do
    =fa_icon 'edit'

.groups
  %p.groups__name-group
    text-group 
  %p.groups__name-text
    さようなら

  %p.groups__name-group
    sample-group
  %p.groups__name-text
    hello!

  %p.groups__name-group
    example
  %p.groups__name-text
    まだメッセージがありません。